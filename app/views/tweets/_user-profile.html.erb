<% if user_signed_in? %>
	<% if current_user.following(user) %>
		<%= button_to "Follow", friendships_path(user_id: user.id), :class -> "btn btn-primary" %>
	<% else %>
		<%= button_to "Follow", friendships_path(user_id: user.id), method: :delete, :class => "btn btn-default" %>
	<% end %>
<% end %>