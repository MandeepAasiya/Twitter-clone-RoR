<section class="section">
  <div class="container">
    <div class="columns">
      <div style="display:flex; flex-direction:column">
        <% if user_signed_in? %>
          <%= render 'profile' %>
        <% end %>
        <%= render 'trends' %>
      </div>
      <div class="column is-half">
       <%= render 'feed' %>
      </div>
    </div>
  </div>
</section>